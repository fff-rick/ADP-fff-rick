addr: ":8080"

worker:
  grpc_addr: ":9090"

db:
  dsn: ""

auth:
  admin_username: "admin"
  # Inject the following values from the runtime Secret; never commit them here.
  admin_password: ""
  secret: ""
  worker_token: ""

llm:
  base_url: "https://api.deepseek.com"
  api_key: ""
  model: "deepseek-v4-pro"

ai_context: "configs/ai/ai_context.yaml"
