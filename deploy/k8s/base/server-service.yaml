apiVersion: v1
kind: Service
metadata:
  name: adp-server
  labels:
    app.kubernetes.io/name: adp-server
    app.kubernetes.io/part-of: adp
spec:
  selector:
    app.kubernetes.io/name: adp-server
  ports:
    - name: http
      port: 8080
      targetPort: http
